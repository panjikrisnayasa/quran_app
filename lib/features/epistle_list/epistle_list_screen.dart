import 'package:flutter/material.dart';

class EpistleListScreen extends StatelessWidget {
  const EpistleListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
